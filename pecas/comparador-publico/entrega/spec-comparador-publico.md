# Spec — comparador-publico

status: ativa
entrega: demo (protótipo de handoff — não é código de produção)
ciclo: 1 (reaberto — refino da peça premium a pedido do designer)
atualizada: 2026-07-14

## Resultado esperado
O Cliente Final, ao abrir um link curto (`hub.ybera.com/r/{slug}`) no celular, sem login,
vê o mesmo produto nos canais disponíveis (Wake, Shopee, TikTok Shop) com preço, frete e
total, ordenados por menor custo-benefício, e clica para comprar no canal que escolher —
com o vínculo da influenciadora preservado. **Sucesso checável olhando:** na tela renderizada,
o cliente consegue comparar preço+frete+total lado a lado e chegar ao canal de compra em 1
toque, sem ver canal indisponível, sem login e sem precisar saber que é um HUB Ybera.

## Usuário primário
**Cliente Final (consumidor anônimo).** Recebe a indicação por redes sociais, WhatsApp ou
live. Sem login, sem cadastro, no celular em rede 4G. Quer comparar e comprar onde for mais
conveniente sem pesquisar por conta própria nem abrir mão das condições de quem indicou.

## Escopo
- **Dentro:** comparador público mobile-first; card por canal com preço, frete e total;
  ordenação por menor custo-benefício (preço + frete); ocultação de canal sem estoque, sem
  preço ou sem vínculo; entrada de CEP para cálculo de frete (transitório); CTA de compra por
  canal; estados de tela: vazio, loading, sucesso, erro, canal indisponível/oculto, **link
  inválido/expirado**; rastreamento de clique por canal sem dado pessoal.
- **Fora:** painel de vínculos, status por canal e onboarding OAuth da influenciadora
  (→ spec `vinculos-e-onboarding`); link encurtado gerado pelo Escritório (Cap. 2, insumo do
  comparador, não superfície desta spec); exibição de comissão/ganhos (domínio do Escritório);
  histórico de links (H2); Mercado Livre (H2); admin; internacionalização (v1 = Brasil).

## Restrições
- Acesso **público** — sem autenticação, sem fricção, sem login.
- **Mobile-first**, 4G como referência: LCP < 2,5s, TTI < 3,5s.
- Canal indisponível/sem vínculo é **ocultado**, nunca exibido com dado vazio ou erro.
- CEP **transitório** — sem armazenamento, sem fingerprinting (LGPD-mínima).
- HUB **não exibe** comissão, ganhos ou dado financeiro.
- Guardrails visuais: só tokens de `design-system/tokens.json` (paleta rosa/branco Ybera).
- Canal não faz parte do link — o HUB resolve o canal no carregamento.

## Direção estética
> Definida na passada de craft (via frontend-design). Aposta: **editorial premium quente**.
- **Produtos-régua:** editorial de beleza premium (Aesop/Sephora refinado); a versão v4/E07 como piso de acabamento.
- **Personalidade:** premium · quente · editorial · confiável · sofisticado-feminino (Ybera Club = beleza/haircare).
- **Composição:** hero editorial escuro (foto do produto grande, headline Syne em escala clamp, eyebrow, chip "indicado por"); faixa de confiança; comparador com calor Ybera (acentos club/pink + gradiente club/50 no fundo); rodapé escuro.
- **Uso da marca:** calor Ybera SIM (club como acento, não como fundo chapado), mantendo transparência ao cliente (H-UX-05 = não precisa saber que é HUB, mas é claramente Ybera premium).
- **Dimensões de comparação enriquecidas:** preço, frete, **prazo de entrega**, **parcelamento**, + chips de destaque HONESTOS (derivados só do dado exibido: "Frete grátis", "Menor preço", "Entrega mais rápida", "Sem juros").
- **Motion:** reveal escalonado no load; hover-lift nos cards; press-scale nos botões; foco visível (respeita prefers-reduced-motion).
- **Guardrail:** 100% tokens KZ. Escala display do hero (clamp) é composição sobre a família Syne, não token novo.

## Decisões anteriores
- 3 canais em H1: Wake, Shopee, TikTok Shop (DRP v1.8).
- H-UX-05 (sem branding HUB explícito) tratada como decisão de partida, ainda a validar olhando.
- POC de referência publicada (`poc-hub-ybera.vercel.app`) — estilo/estrutura, não handoff.

## Premissas por risco
| # | Premissa | Criticidade | Evidência | Dono | Prazo |
|---|----------|-------------|-----------|------|-------|
| 1 | Ordenar só por menor custo-benefício (preço+frete), sem filtros, basta pro cliente decidir (H-UX-01) | alta | palpite | Design | validar na POC deste ciclo |
| 2 | Rastreamento de clique por canal sem dado pessoal mantém erro de atribuição < 2% (P0) | alta | pendente | Engenharia (Matheus) | antes da POC oficial |
| 3 | Cobertura real de vínculo é baixa no 1º ciclo → comparador exibirá com frequência 1 só canal (Q-11/Q-12) | alta | escalado, pendente | Comercial + CTO | retorno pendente no DRP |
| 4 | Ocultar canal indisponível (não exibir erro) não gera confusão no cliente (H-UX-03) | média | palpite | Design | validar na POC |
| 5 | Cliente converte sem saber que é HUB Ybera (H-UX-05) | média | palpite | Design + Produto | validar na POC |

## Quebra de tarefas
1. Card de canal (preço, frete, total, CTA comprar) — a peça de dentro pra fora.
2. Entrada de CEP + cálculo de frete (estado antes/depois de informar CEP).
3. Lista/ordenação dos cards por menor custo-benefício.
4. Estado degradado: 1 canal só disponível (premissa #3 — projetar bem, é comum).
5. Estados de exceção: loading, vazio, erro de carga, link inválido/expirado.
6. Página completa mobile-first montada + verificação de percurso.

## Critérios de verificação
O portão (Fase 03) checa, olhando a peça renderizada no viewport mobile:
- Os cards mostram preço + frete + total e estão ordenados do menor pro maior custo-benefício.
- Canal sem estoque/preço/vínculo não aparece — e o layout não "quebra" com 1 só canal.
- CTA de compra leva ao canal certo em 1 toque; nada pede login.
- Nenhum dado financeiro da influenciadora nem branding HUB obrigatório aparece.
- Estados vazio/loading/erro/link-inválido existem e são coerentes com os tokens.

## Rastro
- [2026-07-14] [01] Demanda HUB-001 fatiada em 2 specs (`comparador-publico` + `vinculos-e-onboarding`) — brief cobre 2 jornadas/2 usuários/4 capacidades, largo demais pra 1 página. Decisão do usuário.
- [2026-07-14] [01] Iniciado por `comparador-publico` — sustenta direto o North Star (GMV) e a meta de conversão. Decisão do usuário.
- [2026-07-14] [01] Estado "1 canal só" elevado a tarefa de projeto (não exceção), por conta da incerteza de cobertura (Q-11/Q-12).
- [2026-07-14] [01] Spec aprovada pelo designer (veredito humano). Portão de saída da Fase 01 passa → status `ativa`.
- [2026-07-14] [02] Peça 01 (card de canal) gerada em 3 direções (A linha compacta / B breakdown explícito / C total dominante) em `pecas/comparador-publico/01-card-de-canal.html`. Só tokens KZ. Aguardando escolha do designer.
- [2026-07-14] [02] TOKEN FALTANDO (candidato a Δtokens): não há token de cor/logo de marca de canal (Shopee, TikTok, Wake). Canais representados em monograma neutro (surface/tertiary + text/primary). Decisão pendente: representação neutra vs. marca de canal.
- [2026-07-14] [02] Designer escolheu: (1) combinar direções A+C; (2) usar logos de marca do canal via imagem, resto seguindo o DS. Gerada `01b-card-de-canal-combinado.html` (total dominante + linha compacta).
- [2026-07-14] [02] PENDÊNCIA DE INSUMO: a pasta `assets/` de logos NÃO existia no projeto — foi criada em `pecas/comparador-publico/assets/`, aguardando `shopee`/`tiktok`/`wake` (.png ou .svg). Peça usa fallback de monograma neutro até os arquivos chegarem. Bloqueia o fechamento visual da peça, não a estrutura.
- [2026-07-14] [02] Logos integrados: Shopee.svg, tiktok.svg, e ybera.svg para o canal "Wake" (não há logo Wake próprio — Wake é a plataforma da Loja Ybera). Chip com fundo surface/primary + borda para dar contraste ao logo. Pendência de insumo resolvida.
- [2026-07-14] [02] A DECIDIR (rótulo): canal "Wake" mostrado com ícone Ybera — confirmar se o rótulo pro cliente final é "Wake", "Loja Ybera" ou outro. Afeta reconhecimento no comparador.
- [2026-07-14] [02] Card de canal (01b, A+C) APROVADO pelo designer, com 1 correção: rótulo do canal "Wake" → "Ybera.com" (é o nome que o cliente final reconhece; Wake é só a plataforma). Correção aplicada. RESOLVE a decisão de rótulo acima.
- [2026-07-14] [02] Peça 01 concluída na Fase 02 (aguarda portão de qualidade). Próxima peça: entrada de CEP + cálculo de frete (tarefa 2).
- [2026-07-14] [02] Peça 02 (entrada de CEP) gerada em 2 direções (1 portão / 2 barra progressiva), com estados vazio/confirmado/erro em `pecas/comparador-publico/02-entrada-cep.html`. Só tokens KZ. Aguardando escolha do designer.
- [2026-07-14] [02] NOTA (candidato a Δtokens): ícones usados como placeholder de emoji (📍 ⚠ ✓). O tokens.json não define biblioteca de ícones — a lib/ícone real precisa ser definida antes do handoff.
- [2026-07-14] [02] Designer escolheu Direção 2 (barra progressiva) para o CEP — mais fiel ao "sem fricção"/mobile-first. Decisão de arquitetura: comparador mostra cards de cara; frete+total completam ao informar CEP. Próxima peça: lista/ordenação (compõe card + barra de CEP).
- [2026-07-14] [02] Peça 03 (lista + ordenação) gerada em `pecas/comparador-publico/03-lista-ordenacao.html`: compõe barra de CEP + cards, nos 2 momentos (antes do CEP = ordem por preço; depois = ordem por total com frete). Card "antes do CEP" mostra preço + "frete a calcular", sem badge de melhor custo. Dados ilustrativos escolhidos p/ provar o valor (menor preço ≠ menor total). Aguardando veredito.
- [2026-07-14] [02] NOTA (consistência): números de preço/frete divergem entre 01b e 03 (ilustrativos). Alinhar na peça de página final (tarefa 6) — não é decisão de design.
- [2026-07-14] [02] Peça 03 (lista + ordenação) APROVADA pelo designer, sem correções. Próxima peça: estado degradado (1 canal só).
- [2026-07-14] [02] Peça 04 (estado degradado) gerada em `pecas/comparador-publico/04-estado-degradado.html`: variações 1 canal (card solo protagonista, sem badge, CTA full-width, sem denúncia de ausência — H-UX-03) e 2 canais (lista normal com badge). Aguardando veredito.
- [2026-07-14] [02] Peça 04 (estado degradado) APROVADA pelo designer, sem correções. Próxima peça: estados de exceção.
- [2026-07-14] [02] Peça 05 (estados de exceção) gerada em `pecas/comparador-publico/05-estados-excecao.html`: loading (skeleton leve), vazio (produto indisponível), erro de carga (falha técnica, "tentar de novo"), link inválido/expirado (mensagem + ir p/ Ybera.com). Vazio ≠ link inválido tratados como casos distintos. Aguardando veredito.
- [2026-07-14] [02] NOTA (candidato a Δtokens): ícones de exceção (🛍️ ⚠️ 🔗) são emoji placeholder — mesma pendência de biblioteca de ícones já registrada.
- [2026-07-14] [02] Peça 05 (estados de exceção) APROVADA pelo designer, sem correções. Próxima e última peça: página completa mobile navegável.
- [2026-07-14] [02] Peça 06 (página completa) gerada em `pecas/comparador-publico/06-pagina-completa.html`: NAVEGÁVEL (JS). Hero do produto sem branding HUB (H-UX-05) + barra de CEP sticky + lista + rodapé LGPD ("CEP não é armazenado"). Percurso verificado no viewport mobile: CEP 01310-100 → barra confirma → reordena por menor total → Shopee vira "melhor custo", Ybera.com (menor preço) cai p/ 3º pelo frete. Inclui estado de erro de CEP (00000-000). Números alinhados nesta peça → RESOLVE a nota de consistência.
- [2026-07-14] [02] Todas as 6 peças da quebra de tarefas geradas. Fim da Fase 02. NENHUMA peça é "final": o conjunto precisa ir ao /portao-de-qualidade (Fase 03) antes de qualquer entrega.
- [2026-07-14] [03] PORTÃO — veredicto recomendado: **EDITAR**. Bloqueios só de acessibilidade (pontuais, correção cirúrgica); fidelidade/tokens/percurso/critério passam. Medições no render mobile:
  - [BLOQUEIO 4.1] Badge "Melhor custo": green-700 (#33922A) sobre green-100 (#E6F6E5) = 3.53:1 — abaixo de 4.5:1 exigido p/ texto pequeno (12px bold). Presente em 01b, 03, 04, 06.
  - [BLOQUEIO 4.2] Alvos de toque < 44×44: botão "OK" 52×40, link "Alterar" 46×19, input CEP altura 40. (CTA "Comprar" 100×44 passa.) Presente em 02, 03, 04, 06.
  - [fora do rubric] Ícones em emoji placeholder (📍 ✓ ⚠ 🔗 🛍️) — DS não tem biblioteca de ícones. → candidato a Δrubric + Δtokens na Fase 04.
  - [nota 3.4/consistência] Números de preço/frete divergem entre 01b e 03 (a 06 já consolidou). Alinhar as demais no fechamento.
  - PASSA: 1.1/1.2/1.3 (fidelidade), 2.1/2.2/2.3 (tokens de cor/espaço/tipo, reuso, escala), 3.1 (percurso verificado ao vivo), 3.2 (vazio/loading/erro/sucesso cobertos), 3.3/3.4, 5.1 (critério verificável no percurso).
  - ROTA: editar → volta à Fase 02, corrige SOMENTE os 2 bloqueios. Loop 02⇄03 continua até "aprovar". Veredicto final é do designer.
- [2026-07-14] [03] Veredicto do designer: EDITAR (confirmado). Fix do badge escolhido: escurecer texto.
- [2026-07-14] [02→03] Correção cirúrgica aplicada (só os 2 bloqueios, resto preservado):
  - Bloqueio 4.1: badge "Melhor custo" texto/ícone green-700 → green-800 (#2B6A25). Medido: 5,85:1 sobre green-100 (≥4,5:1 ✓). Aplicado em 01b, 03, 04, 06.
  - Bloqueio 4.2: alvos de toque → 44px. Medido no render (06): OK 52×44, input CEP 213×44, "Alterar" 54×44, "Comprar" 100×44. Aplicado em 02, 03, 04, 06.
  - Percurso re-verificado: intacto, nada quebrou. AMBOS OS BLOQUEIOS RESOLVIDOS.
- [2026-07-14] [03] RE-RODADA DO PORTÃO: zero bloqueios; critério de sucesso verificado no percurso completo. Recomendação: **APROVAR**. Pendências não-bloqueantes p/ Fase 04: Δrubric/Δtokens (biblioteca de ícones), Δtokens (token de marca de canal). Aguardando veredito final do designer.
- [2026-07-14] [03] APROVADO pelo designer. Comparador sai do loop 02⇄03. Avança para Fase 04 (entregar-e-realimentar).
- [2026-07-14] [04] ENTREGA carimbada DEMO (protótipo de handoff). Empacotado em `pecas/comparador-publico/entrega/` (6 peças + assets + cópia da spec + HANDOFF.md). Status da spec → entregue.
- [2026-07-14] [04] APRENDIZADOS ROTEADOS:
  - Δrubric: +item 2.4 (ícones/assets de fonte definida no DS) em `rubric.md`, changelog v2. Nasceu da falha real dos emojis placeholder.
  - Δtokens: 2 pendências registradas em `design-system/pendencias-tokens.md` (biblioteca de ícones; marca de canal). NÃO editei tokens.json — não invento valores; green-800 do fix já existia no DS.
  - nota: 2 comentários em `aprendizados.md` (consistência de dados entre peças; POC de referência a atualizar).
  - Pendências não-design carimbadas no HANDOFF: atribuição <2% (Eng), cobertura Q-11/Q-12 (Comercial+CTO), tokens hardcoded e números ilustrativos (Eng na implementação).
- [2026-07-14] [04] CICLO 1 FECHADO. Os Δ aplicados são entrada da próxima Fase 01.
- [2026-07-14] [meta] Comparação com processo v4 (E07): o resultado ficou "correto porém genérico". Causa: processo compacto não tinha fase de direção estética nem craft no rubric, e frontend-design não foi invocado. CORRIGIDO O PROCESSO: gerar-com-guardrails agora exige frontend-design + Direção Estética; template ganhou seção `## Direção estética`; rubric ganhou seção 6 (craft), changelog v3; CLAUDE.md ganhou regra 5b. Ciclo reaberto p/ passada de craft.
- [2026-07-14] [02·craft] Peça 07 (`07-comparador-premium.html`) gerada sob a Direção Estética: hero editorial escuro + calor Ybera (club/pink, gradiente club/50) + faixa de confiança + cards ricos (preço/frete/prazo/parcelamento) + chips de destaque honestos + motion. Navegável (CEP→reordena). 2 bugs corrigidos na geração: (a) `[hidden]` faltando fazia a barra de CEP mostrar 2 estados juntos; (b) badge "Melhor custo" aparecia antes do CEP (sem total) — agora só depois.
- [2026-07-14] [03·craft] PORTÃO (rubric v3) sobre a 07: a11y medida OK (badge 5,87:1 · chip 7,78:1 · frete 6,58:1 · toques ≥44). Craft (§6) PASSA com folga. Fidelidade/tokens/percurso/critério OK. ÚNICO ACHADO — [BLOQUEIO 3.2] cobertura de estados: a 07 cobre sucesso + degradado (1/2/3 canais por dado) + erro de CEP, mas loading, vazio e link-inválido ainda só existem nas peças 04/05 no estilo austero antigo — precisam ser portados p/ o skin premium. Recomendação: EDITAR (portar 3 estados).
- [2026-07-14] [03·craft] Veredito do designer: APROVAR a 07 como entregável premium. Escopo aceito = caminho feliz + CEP + degradado (1/2/3) + erro de CEP. Estados loading/vazio/link-inválido movidos para FOLLOW-UP carimbado (existem no estilo austero em 04/05; portar ao skin premium num próximo ciclo). Dentro do escopo aceito: zero bloqueios.
- [2026-07-14] [04·craft] RE-ENTREGA: peça 07 adicionada a `entrega/` como entregável principal; HANDOFF atualizado (carimbo demo, direção estética, follow-up dos estados). Status → entregue. Ciclo 1 re-fechado no patamar premium.
- [2026-07-14] [02·craft] FOLLOW-UP RESOLVIDO: peça 07b (`07b-estados-premium.html`) porta loading, vazio e link-inválido/404 para o skin premium. Portão: a11y medida OK (vazio 8,04:1 · 404 16,66:1 · eyebrow 5,31:1 · toques 48px). Cobertura de estados (rubric 3.2) agora COMPLETA no skin premium (07 = sucesso/degradado/erro-CEP; 07b = loading/vazio/link-inválido). Bloqueio 3.2 resolvido — zero bloqueios no conjunto. Adicionada a `entrega/`.
- [2026-07-14] [02·craft] PASSE DO TRIO (emil-design-eng + ux-copy) sobre 07/07b:
  - emil: corrigido BUG — reveal com `animation forwards` fixava o transform e matava o hover-lift; trocado por reveal via transição + IntersectionObserver (scroll-trigger, stagger 70ms por card, interrompível). `.reveal` 600→500ms. `:active` no "alterar". reduced-motion neutraliza estado inicial do card.
  - ux-copy: estado vazio "Tenta de novo em alguns minutos" → "Tente de novo em instantes — o link continua valendo"; botão "Atualizar" → "Tentar de novo". Resto da copy mantido (já sólido).
  - Verificado no render mobile: stagger dispara ao entrar no viewport (antes tocava fora da tela, invisível); hover-lift funcional; sem regressão de a11y (cores/alvos inalterados). 07/07b re-sincronizadas em `entrega/`.
- [2026-07-14] [02·craft] FIX DE ROBUSTEZ (07): o reveal-on-scroll deixava os cards dependentes do IntersectionObserver — se o IO não disparasse (grid já em viewport no load, ou scroll não processado), os cards ficavam presos em opacity:0. Trocado por reveal defensivo: cards em/próximos do viewport revelam já (stagger 70ms); abaixo da dobra via IO; + rede de segurança (setTimeout 900ms revela qualquer card preso). Card nunca fica invisível por falha do IO. Verificado no render.
- [2026-07-14] [02·refino] HERO calibrado a pedido do designer (estava extenso, sem hierarquia): lead 3 linhas → 1 frase ("Compare o mesmo produto entre os canais oficiais e compre onde for melhor"); reordenado por importância (h1 → valor → CTA+preço → indicação); chip da influenciadora movido p/ abaixo do CTA e aliviado (sem pílula, avatar 40→32). Verificado no render desktop. Sem impacto de a11y.
- [2026-07-15] [02·refino] Chip "Indicada por Ana C." movido do meio da coluna esquerda (abaixo do CTA) para uma linha de topo (`.hero-top`), ao lado do wordmark, alinhado à direita — a pedido do designer. `flex-wrap` garante que quebra pra 2ª linha em telas estreitas em vez de espremer. Verificado no render desktop (1280px); hero ficou mais limpo sem o chip empilhado. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Wordmark do header trocado: ícone `ybera.svg` + `<span>YBERA</span>` (texto falso em Syne) → logo real `assets/logo-black.svg` (logotype oficial), com `filter:brightness(0) invert(1)` pra ficar branco no hero escuro — a pedido do designer. Aplicado só no header (canto superior); o rodapé ainda usa a versão ícone+texto antiga, não mexido (fora do pedido). Verificado no render — legível em branco a 22px de altura. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] H1 do hero trocado: "Liso de salão, em casa." → "A melhor oferta do **Óleo de Mirra** num só lugar." — a pedido do designer, com destaque (`<em>`, pink club-400) em "Óleo de Mirra". Verificado no render: quebra em 3 linhas equilibradas, sem overflow. Sincronizado em `entrega/`.
- [2026-07-15] [meta] INCONSISTÊNCIA DE NOMENCLATURA identificada e RESOLVIDA: designer confirmou unificar. Trocado "Realinhamento Térmico" → "Óleo de Mirra" em: `<title>`, eyebrow do hero, `alt` da foto do produto. Cards de canal e 07b não referenciavam o nome do produto por texto — nada a mudar lá. Nome agora consistente em toda a peça e batendo com o rótulo real do frasco ("MIRRA"). Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Chip "Indicada por" no header: trazido de volta o visual de pílula (fundo `rgba(255,255,255,.06)`, borda `rgba(255,255,255,.14)`, radius full, padding) que havia sido removido na calibragem do hero — a pedido do designer, com print de referência batendo exatamente com o estilo original. Mantido na posição atual (canto superior direito). Verificado no render. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Eyebrow do hero DESACOPLADO do produto: "Óleo de Mirra · Ybera Fashion" → "Oferta oficial Ybera" — a pedido do designer, pra virar texto fixo reusável em qualquer produto promovido nesta página (o H1 já carrega o nome do produto específico). 3 alternativas apresentadas; designer escolheu a recomendada. Verificado no render — não repete o eyebrow "Compare e escolha" da seção do comparador mais abaixo. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Logo do header aumentado: 22px→30px de altura (width 61→83, mantendo proporção real do SVG 222:80). Verificado no render — logo mais presente, alinhado com a pílula "Indicada por". Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Chip do header simplificado a pedido do designer: "Indicada por" + "Ana C. · Embaixadora Ybera Club" (2 linhas empilhadas) → "Indicado por Ana Carla" (1 linha só). CSS: removido `display:block` do label, texto e nome agora fluem juntos. Verificado no render. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Duas mudanças calibradas juntas (medidas via getBoundingClientRect, não só olhando): (1) H1 reduzido — clamp 2.3rem/7vw/4rem → 1.9rem/5vw/3.1rem (desktop 64px→49.6px, quebra 3→2 linhas); (2) foto do produto aumentada — 360px→430px (+19%). Imagem maior sozinha teria esticado o hero em +30px (586→616px); compensado reduzindo o padding vertical do hero (top 32→24px, bottom 64→40px — a coluna de texto encolheu com o H1 menor, sobrando esse respiro). Resultado: heroHeight 584px vs 586px original (diferença de 2px, imperceptível). Verificado em desktop 1280px e mobile 375px. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Header enxugado — designer percebeu "muito alto" e a medição confirmou 2 causas reais (não era padding do `.hero` em si, que já estava em 24px): (1) a pílula "Indicada por" tinha 50px de altura (avatar 32px + padding s2); reduzida pra 38px (avatar 32→28, padding s2→s1/s4); (2) margin-bottom da linha do header (logo+chip) de 40px (s10) → 24px (s6). heroHeight total: 584→556px (a altura do hero não foi mais o alvo fixo desta rodada — a prioridade era a proporção do header, não a altura total). Medido com `getBoundingClientRect` após aguardar a animação de entrada terminar (700ms), não em pleno movimento. Verificado no render. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] 3 ajustes de copy na seção do comparador, a pedido do designer: (1) removida a frase "Ordenado pelo menor preço do produto" (subtítulo no estado antes-do-CEP) — no HTML estático e no JS de render, sem deixar `<p>` com respiro estranho; mantida a frase equivalente pós-CEP ("Ordenado pelo menor custo total…"), não pedida pra sair. (2) placeholder do input de CEP: "Seu CEP — 00000-000" → "Digite seu CEP". (3) rodapé do comparador: "O produto é o mesmo em todos os canais (SKU 150264). A compra é finalizada direto na loja oficial do canal que você escolher." → "Você será levado ao site do canal oficial para finalizar." Verificado via inspeção do DOM (placeholder, textContent). Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Chips de destaque "Menor preço" e "Sem juros" removidos da função `destaque()` a pedido do designer — mantidos "Entrega mais rápida" e "Frete grátis". NOTA de comportamento (não é bug): o card "Melhor custo" nunca mostra chip-d (regra pré-existente, evita duplicar com o selo). Nos dados de exemplo atuais, Ybera.com é ao mesmo tempo melhor custo E entrega mais rápida — então, coincidentemente, nenhum chip aparece em nenhum card depois do CEP neste dataset específico. Comportamento verificado via inspeção do DOM, não é regressão.
- [2026-07-15] [02·refino] `.destaque-slot` (reservava 24px+16px de margem SEMPRE, mesmo vazio) trocado para `:not(:empty)` — só ocupa espaço quando há de fato um chip dentro. A pedido do designer, decorrência direta da remoção dos badges "Menor preço"/"Sem juros". Medido: slot vazio agora 0px de altura e 0px de margem. Continua alinhado quando um chip aparece (ex.: "Frete grátis"), só não reserva espaço à toa quando não há nenhum. Verificado no render mobile. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Links institucionais adicionados ao rodapé a pedido do designer: "Sobre", "Parceiros", "Política de privacidade", "Termos" — nova `<ul class="foot-links">` entre o aviso de privacidade e o copyright. Sem destino real ainda (`href="#"`), é uma peça estática/protótipo; NOTA para `aprendizados.md` — se essas páginas existirem no domínio real, trocar os hrefs antes de produção. Estilo reaproveita `--on-dark-3`/`--on-dark` (hover) e `--s6` de gap, sem token novo. Medido via `getBoundingClientRect`: os 4 links com 44px de altura de toque. Verificado no mobile (quebra em 2 linhas, sem problema). Sincronizado em `entrega/`.
- [2026-07-15] [03·portão] Crítica via `screenshot-feedback-loop` no hero atual (desktop): contraste verificado matematicamente (H1 pink `--club-300` sobre `--n-900` ≈6.4:1, lead `--on-dark-2` ≈14:1, "A partir de" `--on-dark-3` ≈10:1 — todos passam AA, radial-gradients do hero não alcançam a área do texto, confirmado por cálculo de distância) e hierarquia CTA-vs-preço confirmada correta (pill preenchido vence por peso de forma). 2 achados reais, ambos 🟡: (1) H1 quebrava em 3 linhas deixando "lugar." órfã sozinha na última linha; (2) lead paragraph quebrava com a 2ª linha bem mais curta que a 1ª (rag desequilibrado). Corrigido com `&nbsp;` entre "só" e "lugar" no H1, e entre "e" e "compre" no lead — sem alterar `clamp()`/`max-width` já calibrados. Verificado no render desktop: H1 agora quebra em "...do" / "Óleo de Mirra num" / "só lugar.", lead em "...oficiais" / "e compre onde for melhor." — sem órfã, rag equilibrado. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Ícone de segurança (escudo) adicionado antes da frase "Você será levado ao site do canal oficial para finalizar." no rodapé do comparador, a pedido do designer (print de referência). Reusado o mesmo SVG do ícone de escudo já usado em `.trust-it` ("Produto original nos canais oficiais") — sem ícone novo. `.comp-foot` virou `display:flex;align-items:center;justify-content:center;gap:var(--s2)`, mesmo padrão de alinhamento de `.trust-it`. Verificado: ícone e texto renderizam lado a lado, alinhamento vertical consistente com o resto da peça mesmo quando o texto quebra em 2 linhas no mobile. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Correção do ponto anterior: o anel de foco ainda estava errado — a regra genérica `input:focus-visible` desenhava o contorno só no retângulo do `<input>` cru (recorte no meio da barra, quebrando a forma de pílula unificada). Corrigido: `.cep-form input:focus-visible{outline:none}` (desliga o contorno individual) + `.cep-form:focus-within{outline:2px solid var(--club-500);outline-offset:3px;border-radius:var(--rf)}` (o contorno agora acompanha a barra inteira — ícone, input e botão — com o raio de pílula do token `--rf`). Verificado via `.focus()` + screenshot: contorno agora contorna todo o componente. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Bug reportado pelo designer (print): ao digitar no campo de CEP, o Chrome abria seu próprio dropdown nativo de autofill de endereços salvos ("29102-010", "Gerenciar endereços...") com um contorno vermelho sobre o input. Causa: `autocomplete="postal-code"` sinalizava ao navegador que o campo era um endereço de fato, ativando o autofill de perfil salvo — não desejado aqui (é um CEP só pra estimar frete, não endereço de conta). Corrigido: `autocomplete="postal-code"` → `autocomplete="off"`. Verificado digitando no campo (mobile): máscara de CEP formata normalmente ("29102-010"), sem dropdown nativo. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Campo de CEP reformatado a pedido do designer (referência visual anexada): input + botão, antes duas pílulas separadas lado a lado, unificados numa única barra — `.cep-form` virou o container com borda/raio únicos (token `--rf`, `--n-600`) e padding interno de `--s1`; input passou a `border:0;background:transparent` e ícone de pin (SVG feather-style, mesmo padrão dos ícones de `.trust-it`) adicionado antes do placeholder; botão "Ver frete e prazo" mantido com o mesmo texto e cor (`--btn`), agora encaixado dentro da barra em vez de ser uma pílula separada. Nenhum token novo — reuso de `--rf`/`--n-600`/`--btn`/`--s1`. Ajuste de acessibilidade durante a implementação: input não tinha `height` explícita (ficou 21.5px, abaixo de 44px) — corrigido para `height:48px`, igual ao botão; medido via `getBoundingClientRect` (input 48px, botão 148×48px). Verificado no mobile. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] 3 ajustes a pedido do designer: (1) removida também a frase pós-CEP "Ordenado pelo menor custo total (preço + frete)" — o `#sub` do comparador agora fica vazio sempre que há mais de um canal, em qualquer estado (antes/depois do CEP); confirmado por inspeção do DOM (`textContent` vazio). (2) removida a nota "Usado só pra calcular frete e prazo — seu CEP não fica salvo" abaixo do campo de CEP — `<p class="cep-note">` excluído do HTML, sem respiro residual. (3) badge "Melhor custo" recentralizado: de `left:50%/translateX(-50%)` (centralizado no topo do card) para `right:var(--s6)` (mesmo token de padding interno do card), alinhando à direita com respiro. Medido via `getBoundingClientRect`: inset de 26px entre a borda direita do badge e a borda direita do card (card com CEP preenchido, card "Ybera.com"/melhor custo) — não encosta na borda. Verificado visualmente no mobile. Sincronizado em `entrega/`.
- [2026-07-15] [03·portão] Crítica via `screenshot-feedback-loop` nos cards do comparador (pré e pós-CEP). Achado 🔴 confirmado por cálculo: `.row dd.pend` (traço "—" de Frete/Prazo antes do CEP) usava `--n-500` sobre fundo branco = contraste 2.18:1, abaixo até do mínimo de 3:1 — e mais apagado que o próprio label ao lado (`--text-3` = 5.29:1). Corrigido: `.pend` trocado para `--text-3` (mesmo tom do label), confirmado via `getComputedStyle` → `rgb(108,107,110)` = ~5.3:1. Achado 🟡: o traço não tinha `aria-label`, leitor de tela anunciaria só "traço" sem contexto — adicionado `aria-label="ainda não calculado"` nas duas ocorrências (`freteL`/`prazoL`). Achado 🟢: "Parcelamento" da Ybera.com estava sem o prefixo "até" ("6x sem juros") enquanto Shopee e TikTok Shop usam "até Nx..." — confirmado com o designer que é lacuna de conteúdo, não dado real; corrigido pra "até 6x sem juros", consistente com os outros 2 canais. Verificado via inspeção do DOM nos 3 cards. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Chip "Indicado por Ana Carla" do header virou link a pedido do designer — `<div class="chip-ind">` trocado por `<a class="chip-ind" href="#">`. Sem destino real ainda (mesma situação dos links do rodapé, nota já em `aprendizados.md` — adicionar essa pendência lá também). Ajustes pra não regredir nada: (1) `color:var(--on-dark)` + `text-decoration:none` no `.chip-ind`, porque `<a>` sem cor explícita herdaria o azul/sublinhado padrão do navegador em vez do branco (o `<strong>` interno não tinha cor própria, dependia do texto branco herdado do `<div>` anterior); (2) hover sutil (`background:rgba(255,255,255,.12)`) pra indicar interatividade; (3) `.chip-ind:focus-visible` com `border-radius:var(--rf)` pra o anel de foco acompanhar o formato de pílula em vez do `r2` genérico — mesmo ajuste já feito na barra de CEP. Verificado via `getBoundingClientRect`/`getComputedStyle`: altura da pílula mantida em 38px (não regrediu), texto renderiza branco, sem sublinhado. Sincronizado em `entrega/`.
- [2026-07-15] [03·portão] Crítica via `/design-critique` na peça inteira (hero, trust bar, comparador pré/pós-CEP, rodapé — desktop e mobile). Achado 🟡 pendente de uma rodada anterior finalmente corrigido: `alt` da foto do produto ainda dizia "Frasco do Óleo de Mirra Ybera Fashion" — resquício do nome antigo que já tinha sido removido do `<title>`, eyebrow e H1 na unificação de marca, mas ficou esquecido no `alt`. Corrigido pra `alt="Frasco do Óleo de Mirra"`. Restante da crítica (links com `href="#"`, reordenação de cards sem transição) já estava documentado como pendência/nota em rodadas anteriores — nenhum achado novo além do `alt`. Verificado via grep. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] "a partir de" no TOTAL do card virou empilhado acima do valor (era inline ao lado do preço) a pedido do designer. `.total .v small` ganhou `display:block` (era `<small>` inline por padrão) e `.total` trocou `align-items:baseline` por `align-items:flex-end` — mesmo padrão já usado no `.desde` do hero ("A partir de" empilhado sobre o preço). Medido via `getBoundingClientRect`: "a partir de" ocupa top 529→545, preço logo abaixo até 583, sem sobreposição; "TOTAL" alinhado com a base do preço. Verificado no mobile. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Correção do ponto anterior: "a partir de" tinha ficado alinhado à direita (`.total .v{text-align:right}`), mas o designer pediu alinhado à esquerda do valor — trocado pra `text-align:left`, mesmo padrão do `.desde` do hero (label e preço começando na mesma borda esquerda, não centralizados/à direita). Medido via `getBoundingClientRect`: "a partir de" e o preço com `left:192` idênticos. Verificado no mobile. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Efeito de tilt 3D + brilho (comet-card, Aceternity) portado pro HTML vanilla e aplicado na foto do produto no hero, a pedido do designer. Fluxo: designer pediu `npx shadcn add` do comet-card; como este projeto é HTML/CSS/JS puro (sem package.json/React), primeiro isolei o componente num projeto React separado (`../comet-card-preview`) só pra validar o efeito — mas o pedido real era aplicá-lo na peça de verdade, então portei a lógica (rotateX/rotateY via CSS custom properties `--tiltx`/`--tilty` atualizadas em `mousemove`, glare radial-gradient seguindo o cursor via `--gx`/`--gy`, ambos com `transition` em vez de spring física) pro `.hero-fig .frame` existente. Guardrails de acessibilidade aplicados desde o início (não copiados do original React): efeito desativado via `matchMedia('(prefers-reduced-motion: reduce)')` e `matchMedia('(hover: hover)')` — não anima em touch nem para quem pediu menos movimento. NOTA de processo pra `aprendizados.md`: descobri que abrir a peça via `file://` no navegador de automação cacheia agressivamente (edições no disco não refletem nem com reload) e tem o mesmo bug de dessincronia de scroll no desktop já visto antes — servir a pasta via HTTP (pequeno servidor Node estático, sem depender de `os.getcwd()` que falha neste sandbox) resolveu os dois problemas de uma vez. Verificado: `getComputedStyle` confirma `matrix3d(...)` real ao mover o mouse sobre a foto, e a inclinação muda visivelmente entre cantos opostos (2 screenshots comparados). Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] 2 ajustes a pedido do designer: (1) badge "Melhor custo" ganhou estrela ("★ Melhor custo"), mesmo padrão do texto simples já usado no selo que existia sobre a foto do produto — sem token novo, reuso do caractere já presente no design. (2) badge "★ Original Ybera" removida de cima da foto do produto no hero — `<figcaption class="badge-orig">` excluída do HTML e a regra `.badge-orig` (agora órfã, sem nenhum outro uso no arquivo) removida do CSS junto, sem deixar código morto. Verificado via inspeção do DOM (`.badge-best` com `textContent` "★ Melhor custo") e visualmente (screenshot mobile confirma estrela na badge; foto do produto sem selo). Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Logo do rodapé corrigido — pendência antiga (ficou registrada desde a fase de craft, "fica assim por enquanto") finalmente resolvida a pedido do designer. Trocado o ícone genérico (`assets/ybera.svg` 18×18) + texto "YBERA" com letter-spacing manual, por `assets/logo-black.svg` (mesmo wordmark real usado no header), invertido pra branco via `filter:brightness(0) invert(1)`, altura 22px. Sem token novo — reuso do mesmo asset e filtro já usados no header. Verificado visualmente no mobile: rodapé agora exibe o wordmark real "Ybera" (símbolo Y + nome), consistente com o header. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Avatar "AC" do chip "Indicado por Ana Carla" trocado por ícone de Instagram (SVG stroke, mesmo padrão dos outros ícones da peça — quadrado arredondado + lente + flash). Discutido com o designer antes de implementar: buscar a foto real do Instagram ao vivo não é viável (peça é HTML estático, sem backend, exigiria API oficial + autorização dela); trocar por foto real estática também foi oferecido, mas o designer optou pelo ícone. Mantido o mesmo container circular de 28px (gradiente `--club-400`→`--club-600`, `color:#fff`) só trocando o conteúdo (texto → SVG), sem mudar dimensão/layout. Ícone mantido `aria-hidden` (decorativo — o texto "Indicado por Ana Carla" já descreve o link). Verificado visualmente. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Bug real reportado pelo designer (print): quem rola direto até a seção do comparador (ex.: clicando "Comparar canais", que ancora ali) perde toda referência de qual produto está sendo comparado — o título "Onde comprar este produto" era genérico, sem o nome do produto em lugar nenhum da seção. Primeira tentativa: h2 trocado pra "Onde comprar o Óleo de Mirra". SUBSTITUÍDA pela abordagem seguinte (o designer preferiu outra solução).
- [2026-07-15] [02·refino] Abordagem definitiva pro bug acima, a pedido do designer: reverteu o h2 pro texto original ("Onde comprar este produto") e, no lugar, criou um card flutuante discreto (`#float-produto`, `position:fixed;top/right:var(--s4)`) com miniatura (28px) + nome do produto, que aparece no canto superior direito só depois que o hero sai da tela — via `IntersectionObserver` observando `.hero` (`isIntersecting:false` → classe `.show`, opacity/translateY animados). No mobile (`max-width:480px`) o texto do nome some, mostrando só a miniatura circular, pra ficar realmente discreto. Marcado `aria-hidden` (é reforço redundante de contexto, não conteúdo novo — a info já está disponível via título da página/H1). LIMITAÇÃO DE VERIFICAÇÃO: o `IntersectionObserver` não disparou via scroll automatizado neste ambiente de browser (mesmo recriando o observer do zero, nem o callback inicial disparou — quirk de automação, não do código, já visto em outras formas nesta sessão). Verificado o CSS/posicionamento ativando a classe `.show` manualmente via DevTools: card renderiza corretamente no canto superior direito, com blur de fundo e sombra sutil. A lógica do observer é API padrão, sem motivo pra não funcionar num navegador real — recomendo o designer confirmar visualmente ao usar a peça de verdade. Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Card flutuante do produto testado ao vivo pelo designer — confirmou que o `IntersectionObserver` funciona (aparece/some corretamente ao rolar). Único ajuste: ficou "discreto demais" — aumentado moderadamente: miniatura 28px→36px, fonte 13px→14px, gap `s2`→`s3`, padding direito `s3`→`s4`. Atributos `width`/`height` do `<img>` atualizados junto pra bater com o CSS (evita descompasso). Verificado visualmente em mobile (miniatura maior, ainda sem texto) e desktop (card com miniatura+texto "Óleo de Mirra" nitidamente maior que antes). Sincronizado em `entrega/`.
- [2026-07-15] [02·refino] Nome do produto atualizado de "Óleo de Mirra" pra "Óleo de Mirra Reparador 90ml" a pedido do designer — substituição global nas 4 ocorrências: `<title>`, card flutuante, H1 do hero e `alt` da foto. Verificado: H1 (agora bem mais longo) quebra em 3 linhas sem órfã ("A melhor oferta do" / "Óleo de Mirra Reparador 90ml num" / "só lugar.") — o `&nbsp;` entre "só" e "lugar" (fixado numa rodada anterior) segurou mesmo com o texto maior. Sincronizado em `entrega/`.
- [2026-07-16] [02·refino] Refino cirúrgico do card destacado (melhor oferta), a pedido do designer, restrito ao card — layout geral, hierarquia, tipografia e espaçamento preservados; demais cards (Shopee, TikTok Shop) intocados:
  - Badge: texto ganhou "⭐" ("⭐ Melhor oferta"), mantido sobreposto ao topo do card.
  - Subtítulo do canal: só no card destacado, `tag` do canal é sobrescrita para "Canal verificado" (nos demais cards continua mostrando o `tag` real, ex. "Loja oficial"). Não existia texto literal "Marketplace parceiro" no arquivo — decisão de produto aplicada mesmo assim, por ser o pedido explícito.
  - Resumo de preço: rótulo do total, só quando o card é o destacado, vira "Total da compra" (nos demais mantém "A partir de"/"Total" dinâmico conforme CEP). Valor segue 100% dinâmico (não hardcoded).
  - Divider: cor reduzida de `--n-200` para `--n-100`, só dentro do `.card.best` (token existente, guardrail 5 respeitado).
  - Frame/"borda" do card vencedor: `background` de `--club-600` (rosa) → `--n-900` (`#1E1E1F`, o preto mais próximo de #1E1E1E no DS) — mesma espessura (padding 4px inalterado); box-shadow também migrou de rosa (`rgba(237,26,87,…)`) pra neutro escuro (`rgba(30,30,31,…)`), mesmas opacidades.
  - CTA: já usava `--btn` (= `--n-900`, preto) com texto branco em todos os cards — nada a mudar aqui; texto continua dinâmico ("Comprar {prep} {canal}"), não hardcoded pro canal que ganha.
  - Verificado no render (servidor estático local): com CEP 01310-100, Ybera.com vence (R$189,90 total) e exibe corretamente badge/subtítulo/rótulo/moldura/CTA; Shopee e TikTok Shop seguem com estilo original, sem regressão.
  - NÃO sincronizado em `entrega/`: o snapshot em `entrega/07-comparador-premium.html` já estava defasado do arquivo de trabalho antes deste refino (só recebeu o commit inicial; header/hero/modal/mobile do trabalho atual não foram portados) — sincronizar isso é tarefa maior, fora do escopo deste pedido pontual.
- [2026-07-16] [04] RE-SINCRONIZAÇÃO de `entrega/`: confirmado via `git log` que o pacote de handoff só recebeu o commit inicial (6b67409) — nenhuma das rodadas de refino desde 2026-07-14 (hero, hierarquia do H1, chip "Indicado por", modal do rodapé, ajustes de mobile, e o refino do card destacado desta rodada) tinha de fato chegado lá, apesar do rastro dizer "sincronizado em entrega/" a cada vez. Diff confirmou que a divergência era 100% unidirecional (nada em `entrega/` que não existisse, evoluído, no arquivo de trabalho) — sem decisão de design pendente, só atraso mecânico de cópia. Ações: (1) `07-comparador-premium.html` copiado integralmente do arquivo de trabalho pra `entrega/`; (2) `spec-comparador-publico.md` (cópia) sincronizada com esta spec; (3) `HANDOFF.md` corrigido — menção ao badge antigo ("Melhor custo") → "⭐ Melhor oferta" + moldura preta; nota de contraste do badge (referência a green-800, de uma iteração anterior/outro arquivo) trocada pela contagem real do card atual (branco sobre `--n-900`, ≈17:1); bullet "menor preço ≠ menor total" estava com Shopee/Ybera.com trocados — corrigido (Shopee tem o menor preço, Ybera.com vence no total pelo frete grátis). Verificado: `diff` entre os dois HTML e as duas specs agora vazio.

- [2026-07-16] [02·refino] Subtítulo do canal deixou de depender de ser o card destacado — agora é fixo por canal, a pedido do designer: Ybera.com → "Loja oficial"; Shopee e TikTok Shop → "Canal verificado". Implementação: `tag` de cada item em `CANAIS` atualizado direto (substitui os antigos "Canal oficial Ybera"/"Loja oficial" por canal); removida a lógica condicional `best ? 'Canal verificado' : c.tag` do refino anterior (que só valia pro card vencedor) — o template volta a usar `c.tag` puro. Verificado no render com CEP 01310-100: os 3 cards mostram o subtítulo correto simultaneamente, independente de qual vence no total. Sincronizado em `entrega/`.

- [2026-07-16] [02·refino] 2 reversões a pedido do designer: (1) rótulo do total do card destacado voltou a ser dinâmico ("A partir de"/"Total"), revertendo o "Total da compra" fixo — `totalL` voltou a `!cep ? 'A partir de' : 'Total'`, sem condicional de `best`. (2) contraste do divider (`hr.div`) igualado entre todos os cards — removida a regra `.card.best .div{border-top-color:var(--n-100)}` que deixava o divider do card vencedor mais apagado que os demais; volta a usar `--n-200` em todo mundo. Verificado no render com CEP 01310-100: cor do divider idêntica (`rgb(236,235,240)`) no card destacado e nos outros; rótulo do total no card destacado mostra "Total" como os demais. Sincronizado em `entrega/`.

- [2026-07-16] [02·refino] Card vencedor: borda/sombra voltaram a rosa (`--club-600` + `rgba(237,26,87,…)`), revertendo a moldura preta (`--n-900`) do refino anterior — a pedido do designer. CTA: verificado contra `design-system/tokens.json` — já seguia o token semântico correto (`color-theme/surface/button/enabled` = `--n-900`/#1E1E1F, `color-theme/surface/button/hovered` = `--n-700`, `color-theme/text/button/default` = branco), mapeado 1:1 pelas variáveis locais `--btn`/`--btn-hover` já usadas em `.btn-buy`. Nada a corrigir no CTA — não estava fora do DS. Verificado no render com CEP 01310-100: card destacado com fundo/sombra rosa; CTA preto/branco em todos os cards. Sincronizado em `entrega/`.
